-  **Execution environment:** Run the application, install dependencies, and execute all tests exclusively inside the Docker Compose containers. Do not use or modify the host system Ruby, Bundler, or gems. Use `docker compose run --rm interview-dev ...` for one-off commands/tests, or `docker compose up` followed by `docker compose exec interview-dev ...` when the service is running.
